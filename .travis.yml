language: node_js
node_js:
  - 12.13.0
install:
  - npm install -g firebase-tools
after_success:
  - firebase deploy
branches:
  only:
    - master
script:
  - echo "Deploy"
deploy:
  provider: firebase
  skip_cleanup: true
  token:
    secure: $FIREBASE_TOKEN
  project: $FIREBASE_PROJECT